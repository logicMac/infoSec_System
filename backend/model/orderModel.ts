import db from "../db";

const orderModel = {
    orderProduct: async(id) => {
        try {
            
        } catch (error) {
            console.log(error);
        }
    }
}

export default orderModel;